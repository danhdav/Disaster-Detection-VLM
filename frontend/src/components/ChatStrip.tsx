export function ChatStrip() {
  return (
    <footer className="chat-strip" role="region" aria-label="Chat">
      <div className="chat-strip-inner">
        <span className="chat-strip-placeholder">Chat — coming soon</span>
      </div>
    </footer>
  );
}
